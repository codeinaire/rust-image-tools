---
import '../styles.css'
import { BASE_URL } from '../constants'

interface Props {
  title: string
  description: string
  canonical?: string
  /** When true, skip the default WebApplication JSON-LD (page provides its own). */
  skipDefaultSchema?: boolean
}

const { title, description, canonical, skipDefaultSchema = false } = Astro.props

const webAppSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: 'Image Toolz',
  description:
    'Convert images between PNG, JPEG, WebP, GIF, BMP, TIFF, ICO, QOI, and TGA formats. HEIC input supported. Runs entirely in your browser\u2014no server uploads.',
  applicationCategory: 'UtilitiesApplication',
  operatingSystem: 'Any (browser-based)',
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
  browserRequirements: 'Requires WebAssembly support',
  url: BASE_URL,
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="robots" content="index, follow" />
    {canonical && <link rel="canonical" href={canonical} />}

    <!--
      Favicon: browsers auto-discover /favicon.ico from the site root.
      Place favicon.ico and apple-touch-icon.png in web/static/ when ready.
      TODO: Design favicon and apple-touch-icon assets.
    -->

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    {canonical && <meta property="og:url" content={canonical} />}
    <meta property="og:image" content={`${BASE_URL}/og-image.png`} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={`${BASE_URL}/og-image.png`} />
    <meta name="theme-color" content="#05050f" />

    <!-- Structured Data: WebApplication (skipped when page provides its own) -->
    {
      !skipDefaultSchema && (
        <script type="application/ld+json" set:html={JSON.stringify(webAppSchema)} />
      )
    }

    <!-- Google Fonts: Space Grotesk (ticker) + Orbitron (display) + Share Tech Mono (body) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@700&family=Orbitron:wght@400;700;900&family=Share+Tech+Mono&display=swap"
      rel="stylesheet"
    />

    <!-- Per-page structured data (e.g. FAQ) -->
    <slot name="head" />
  </head>
  <body class="min-h-screen">
    <slot />
  </body>
</html>
